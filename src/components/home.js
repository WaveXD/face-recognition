import React from 'react';
import Navbar from './Navbar';  // Make sure to replace 'Navbar' with the correct path
import Main from './main';  // Replace 'MainContent' with the correct path

function Home() {
  return (
    <div>
      <Navbar />
      <Main />
    </div>
  );
}

export default Home;
